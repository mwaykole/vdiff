# Standalone Kubernetes deployment for dfastllm
# Use this for simple deployments without KServe or llm-d
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dfastllm-standalone
  labels:
    app: dfastllm
    deployment-type: standalone
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dfastllm
      deployment-type: standalone
  template:
    metadata:
      labels:
        app: dfastllm
        deployment-type: standalone
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: dfastllm
          image: quay.io/your-org/dfastllm:latest
          imagePullPolicy: Always
          
          args:
            - "--model"
            - "GSAI-ML/LLaDA-8B-Instruct"
            - "--port"
            - "8000"
            - "--enable-apd"
            - "--trust-remote-code"
          
          ports:
            - containerPort: 8000
              name: http
          
          resources:
            requests:
              cpu: "4"
              memory: "16Gi"
              nvidia.com/gpu: "1"
            limits:
              cpu: "8"
              memory: "32Gi"
              nvidia.com/gpu: "1"
          
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 10
          
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 120
            periodSeconds: 30
          
          volumeMounts:
            - name: shm
              mountPath: /dev/shm
      
      volumes:
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: 2Gi
      
      nodeSelector:
        nvidia.com/gpu.present: "true"
      
      tolerations:
        - key: "nvidia.com/gpu"
          operator: "Exists"
          effect: "NoSchedule"

---
apiVersion: v1
kind: Service
metadata:
  name: dfastllm-standalone
  labels:
    app: dfastllm
spec:
  type: LoadBalancer
  selector:
    app: dfastllm
    deployment-type: standalone
  ports:
    - name: http
      port: 8000
      targetPort: 8000
